#include<string.h>
#include<string>
using namespace std;

int main()
{
    char str[1024];
    if(strcmp(str,"hello") && (strcmp(str,"world")==0)){  
    }  
    char *s1;
    char *s2;
    if(memcmp(s1, s2, 3)){   
    }

    string a, b;
    if(a==b){}
    return 0;
}
